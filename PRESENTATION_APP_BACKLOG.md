# Presentation App Backlog

## Deferred UI Work

- Add a dedicated processing state for rover-path generation so the UI can explicitly show route solving rather than appearing idle during the first path build.
- Design a mission-style loading treatment for long-running scene computations, including animated status feedback tied to surface routing and target chaining.
